# Admin API

Admin APIs manage tenants, users, roles, permissions, clients, scopes, identity providers, sessions, tokens, consents, signing keys, audit events, security events, blocked IPs, and security policies. Responses use RFC 9457 ProblemDetails for errors and never return EF entities directly.
