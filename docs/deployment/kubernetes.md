# Kubernetes Deployment

Kubernetes manifests define Deployment, Service, Ingress, ConfigMap, Secret template, HPA, PDB, NetworkPolicy, ServiceAccount, and probes. Containers run non-root, drop Linux capabilities, avoid privilege escalation, and request bounded CPU/memory.
