# Docker Deployment

Use `deploy/docker/docker-compose.yml` with environment variables from `.env`. Secrets in `.env.example` are names only and must be replaced locally. Health checks gate dependent services.
